# Observer Project

This folder is the project root for the observer code and notebooks.

- Virtual env: `.venv`
- Notebooks: open from this folder so paths are relative to project root.

Created and configured basic project files to make this folder a reproducible workspace.
